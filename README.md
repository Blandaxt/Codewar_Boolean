# Codewar_Boolean Value

Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

##Sample Test

Sample Tests:

Test.assertEquals(boolToWord(true), 'Yes')
Test.assertEquals(boolToWord(false), 'No')
